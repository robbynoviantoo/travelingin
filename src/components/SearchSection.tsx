import SearchBox from "./Hero/SearchBox"; // Sesuaikan path kalau perlu

export default function SearchSection() {
  return (
    <section className="hidden relative xl:flex justify-center">
      <SearchBox />
    </section>
  );
}
